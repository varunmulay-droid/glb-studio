# ── Development Dockerfile (hot-reload, not for HF Spaces) ────────────────────
FROM node:20-alpine

WORKDIR /app

# Install deps
COPY package.json ./
RUN npm install --legacy-peer-deps

# Source will be volume-mounted
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
